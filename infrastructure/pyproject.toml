[project]
name = "infrastructure"
version = "0.1.0"
description = "CDK Infrastructure for Bubble Tea Ranker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.100.0",
    "constructs>=10.0.0",
    "python-dotenv>=1.0.0",
]
